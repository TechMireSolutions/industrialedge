import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function run() { console.log('Products:', await prisma.product.findMany({ select: { id: true, images: true }, take: 3 })); console.log('Offers:', await prisma.offer.findMany({ select: { id: true, image: true }, take: 3 })); console.log('Heroes:', await prisma.heroSlide.findMany({ select: { id: true, imageId: true, backgroundImageId: true }, take: 3 })); console.log('Media:', await prisma.mediaAsset.findMany({ select: { id: true, storagePath: true, filename: true }, take: 3 })); } run().finally(() => prisma.$disconnect());
